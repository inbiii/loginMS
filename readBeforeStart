you have to create a file named .env with these criteria of your own to get this app to work and two different dynamodb tables

AWS_ACCESS_KEY_ID= (aws access key)
AWS_SECRET_ACCESS_KEY= (aws secret access key)
TOKEN_KEY= (random string - type w/e)
GMAIL_USER= (gmail email address)
GMAIL_PASS= (gmail pass)
EMAIL_KEY= (random string - type w/e)
AWS_REGION= (amazon web region like us-west-1)
AWS_EP= (amazon end point)
PORT = (Your Port Number)
USER_TABLE= (name of your user table)
ACCOUNT_TABLE = (name of your account table)
